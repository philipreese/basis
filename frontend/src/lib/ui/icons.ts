// Central icon registry — import all icons from here, never directly from lucide-svelte.
// Semantic names map intent to icon choice; swap the right-hand side to restyle the whole app.

// ── Navigation ────────────────────────────────────────────────────────────────
export { List          as IconPositions      } from 'lucide-svelte';
export { Zap           as IconOpportunities  } from 'lucide-svelte';
export { TrendingUp    as IconPerformance    } from 'lucide-svelte';
export { Settings      as IconSettings      } from 'lucide-svelte';
export { BookOpen      as IconBooks         } from 'lucide-svelte';

// ── Status / alerts ───────────────────────────────────────────────────────────
export { AlertCircle   as IconCritical      } from 'lucide-svelte';
export { AlertTriangle as IconWarning       } from 'lucide-svelte';
export { Info          as IconInfo          } from 'lucide-svelte';
export { CheckCircle2  as IconSuccess       } from 'lucide-svelte';

// ── Actions ───────────────────────────────────────────────────────────────────
export { X             as IconClose         } from 'lucide-svelte';
export { Check         as IconConfirm       } from 'lucide-svelte';
export { ArrowLeft     as IconBack          } from 'lucide-svelte';
export { RefreshCw     as IconRefresh       } from 'lucide-svelte';

// ── UI chrome ─────────────────────────────────────────────────────────────────
export { ChevronDown   as IconChevronDown   } from 'lucide-svelte';
export { ChevronUp     as IconChevronUp     } from 'lucide-svelte';
export { Lock          as IconLock          } from 'lucide-svelte';
export { Sun           as IconLightMode     } from 'lucide-svelte';
export { Moon          as IconDarkMode      } from 'lucide-svelte';
