/// <reference types="vite/client" />
// Vite's ambient types, for `import.meta.env` (registerServiceWorker.ts uses
// PROD to keep the worker out of the dev server). Picked up by tsconfig's
// existing "src/**/*.d.ts" include.
